name: Build Redmi K40 Pro SukiSU Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Build Environment
      run: |
        sudo apt-get update
        sudo apt-get install -y bc build-essential libelf-dev libssl-dev flex bison ccache libncurses-dev texinfo zip lld llvm
        mkdir -p $GITHUB_WORKSPACE/kernel

    - name: Download Reliable Lightweight Clang
      run: |
        cd $GITHUB_WORKSPACE
        git clone https://github.com/Panchajanya1999/azure-clang.git --depth=1 clang

    - name: Download Kernel Source
      run: |
        cd $GITHUB_WORKSPACE/kernel
        git clone https://github.com/LineageOS/android_kernel_xiaomi_sm8350.git -b lineage-21 --depth=1 .

    - name: Integrate SukiSU-Ultra & Customize Name
      run: |
        cd $GITHUB_WORKSPACE/kernel
        
        # 1. 直接原地克隆到源码目录中
        rm -rf drivers/kernelsu
        git clone https://github.com/SukiSU-Ultra/SukiSU-Ultra.git drivers/kernelsu
        
        # 原地改写 Makefile
        sed -i '/kernelsu/d' drivers/Makefile
        echo "obj-y += kernelsu/" >> drivers/Makefile
        
        # 2. 找到配置文件并原地覆盖到标准的 arch 路径下
        REAL_DEFCONFIG=$(find arch/arm64/configs/ -name "*haydn*" | head -n 1)
        if [ -z "$REAL_DEFCONFIG" ]; then
          REAL_DEFCONFIG=$(find vendor/ -name "*haydn*" | head -n 1)
        fi
        cp $REAL_DEFCONFIG arch/arm64/configs/haydn_defconfig
        
        # 【名字修改】
        sed -i 's/CONFIG_LOCALVERSION=.*/CONFIG_LOCALVERSION="-SUkiSU-Ultra-K40Pro-By-🐾白墨🐾Whiteink🐾"/g' arch/arm64/configs/haydn_defconfig
        
        # 3. 注入配置与排毒
        echo "CONFIG_KALLSYMS=y" >> arch/arm64/configs/haydn_defconfig
        echo "CONFIG_KALLSYMS_ALL=y" >> arch/arm64/configs/haydn_defconfig
        echo "CONFIG_RMNET_SHS=n" >> arch/arm64/configs/haydn_defconfig
        echo "CONFIG_DATARMNET_EXT=n" >> arch/arm64/configs/haydn_defconfig

    - name: Start Compilation
      run: |
        cd $GITHUB_WORKSPACE/kernel
        
        # 环境变量导入
        export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
        export ARCH=arm64
        export SUBARCH=arm64
        
        # 【史诗级修改】彻底去掉 O=out，让它在同一个坑里生根发芽！
        make haydn_defconfig
        
        make -j$(nproc --all) \
          CC=clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
          LD=ld.lld \
          AR=llvm-ar \
          NM=llvm-nm \
          OBJCOPY=llvm-objcopy \
          OBJDUMP=llvm-objdump \
          STRIP=llvm-strip

    - name: Pack with AnyKernel3
      run: |
        cd $GITHUB_WORKSPACE
        git clone https://github.com/osm0sis/AnyKernel3.git AnyKernel3
        sed -i 's/device.name1=.*/device.name1=haydn/g' AnyKernel3/anykernel.sh
        
        # 原地编译的文件直接在原产地拷贝
        cp kernel/arch/arm64/boot/Image AnyKernel3/
        cd AnyKernel3
        zip -r9 ../SukiSU-Kernel-K40P.zip *

    - name: Upload Kernel Zip
      uses: actions/upload-artifact@v4
      with:
        name: SukiSU-Ultra-Kernel-RedmiK40Pro
        path: SukiSU-Kernel-K40P.zip
